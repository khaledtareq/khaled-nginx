on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v3

      - name: "Login to docker hub"
        run: docker login --username khaledtarek --password dckr_pat_cDH3n4LSk80wRdk85DLsl5AHHvY
